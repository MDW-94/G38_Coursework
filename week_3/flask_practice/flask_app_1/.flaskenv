FLASK_DEBUG=true
FLASK_RUN_PORT=4999

# Flask's default port is 5000
# We specify a different one with the above

# the FLASK_DEBUG=true is the python dotenv package
# this allows us to refresh our browser and 
# apply the changes we make
# to our code 