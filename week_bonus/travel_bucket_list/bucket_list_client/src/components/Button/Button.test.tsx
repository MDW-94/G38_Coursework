import { render } from "@testing-library/react";

describe("Button", () => {
  it("display a label", () => {
    render();
  });
});

test("maths works", () => {
  expect(2 + 2).toEqual(4);
});

// bucket_list_client> npm test
